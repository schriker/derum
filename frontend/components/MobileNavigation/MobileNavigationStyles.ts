import { makeStyles } from '@material-ui/core';

const useMobileNavigationStyles = makeStyles(() => ({
  root: {
    position: 'fixed',
    bottom: 0,
    left: 0,
    right: 0,
  },
}));

export default useMobileNavigationStyles;
