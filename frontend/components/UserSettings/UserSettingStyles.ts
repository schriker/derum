import { makeStyles } from '@material-ui/core';

const useUserSettingsStyles = makeStyles({
  gutters: {
    paddingLeft: 0,
    paddingRight: 0,
  },
  secondaryAction: {
    right: 0,
  },
});

export default useUserSettingsStyles;
