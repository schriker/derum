import React from 'react';
import badge from '../../public/owner_badge.svg';
import Badge from './Badge';

const OwnerBadge = () => {
  return <Badge title="Właściciel" image={badge} />;
};

export default OwnerBadge;
