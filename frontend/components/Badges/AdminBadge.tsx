import React from 'react';
import badge from '../../public/admin_badge.svg';
import Badge from './Badge';

const AdminBadge = () => {
  return <Badge title="Admin" image={badge} />;
};

export default AdminBadge;
