import React from 'react';
import badge from '../../public/moderator_badge.svg';
import Badge from './Badge';

const ModeratorBadge = () => {
  return <Badge title="Moderator" image={badge} />;
};

export default ModeratorBadge;
