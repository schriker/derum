import React from 'react';
import badge from '../../public/super_badge.svg';
import Badge from './Badge';

const SuperBadge = () => {
  return <Badge title="Wyróżniony" image={badge} />;
};

export default SuperBadge;
