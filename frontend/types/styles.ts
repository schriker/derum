export type ChatMessageItemStyles = {
  selectedUser?: number;
  isSelected?: boolean;
  userColor: string;
};

export type CommentsItemStyles = {
  level?: number;
  userColor: string;
};

export type RoomAvatarSidebarStyles = { isActive: boolean };
export type SidebarStyles = { isActive: boolean };
export type VoteStyles = { justifyContent: string };
