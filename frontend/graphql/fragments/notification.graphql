fragment NotificationFragment on Notification {
  id
  url
  createdAt
  objectType
  objectId
  parentId
  readed
  triggeredBy {
    ...AuthorFragment
    photo {
      ...PhotoFragment
    }
  }
}
